# Operating notes

Mission Control is the Task Book + systems board.

Studio Brain remains canonical operational memory.
Studio Go remains the mobile creative room.

Do not merge Amber and Allie.
Do not claim a system is live without evidence.
Do not put API keys, tokens, or passwords in this repo.

When a mission changes status, update `data/seed.json` or POST `/api/missions`.
The live store after first run is `data/state.json` (gitignored).
